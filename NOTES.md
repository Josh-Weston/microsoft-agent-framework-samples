`OpenAIChatCompletionClient`: older, for simple tasks, older models (e.g., gpt-4o, oss); OpenAI Chat Completions API
`OpenAIChatClient`: for newer, agentic/reasoning models (e.g., o1, gemma4); OpenAI Responses API
